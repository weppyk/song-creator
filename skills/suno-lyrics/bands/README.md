# Band / Artist Profiles

Each `.md` file in this directory defines one saved artist or band.
The skill reads all files here and presents them as options at startup.

## How to add a new band

Create a new `.md` file following the template in `_template.md`.
The filename becomes the band's ID (e.g. `hori-dan.md` → "Hoří Dán").
